<% layout('layouts/boilerplate') %>
<h1>Todos las Areas para acampar</h1>
<a href="/campgrounds/new">Agrega Un area</a>

<% for( let campground of campgrounds) {%>
<!-- <div class="card" mb-3 mt-3 style="width: 18rem">
  <img src="<%= campground.image %> " class="card-img-top" alt="camp" />
  <div class="card-body">
    <h5 class="card-title"><%= campground.title %></h5>
    <p class="card-text"><%= campground.description %></p>
    <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary"
      >Conoce más</a
    >
  </div>
</div> -->
<div class="card" mb-3>
  <div class="row">
    <div class="col-md-4">
      <img class="img-fluid" src="<%= campground.image %> " alt="" />
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title"><%= campground.title %></h5>
        <p class="card-text"><%= campground.description %></p>
        <p class="card-text">
          <small class="text-muted"><%= campground.location %> </small>
        </p>
        <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary"
          >Conoce más</a
        >
      </div>
    </div>
  </div>
</div>
<% } %>
